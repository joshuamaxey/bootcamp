// toString(radix) takes a number and converts it from one base to another
// parseInt(string, radix) takes a given string and converts it to an integer (base 10) using the specified radix

function decimalToBinary(blob) {
    return blob.toString(2); // 2 means convert to base 2
}
